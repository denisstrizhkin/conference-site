{% extends "base.jinja" %}
{% block title %}{{ title }}{% endblock %}
{% block main %}
<h4 class="mb-3 text-center">Регистрационная форма:</h4>
<div class="d-flex justify-content-center">
<div class="col-md-8">
<form class="needs-validation" novalidate>
    <div class="row g-3">
        <div class="col-sm-4">
            <label class="form-label">Фамилия
                <input type="text" class="form-control" placeholder value required>
            </label>
            <div class="invalid-feedback">Это поле необходимо заполнить</div>
        </div>
        <div class="col-sm-4">
            <label class="form-label">Имя
                <input type="text" class="form-control" placeholder value required>
            </label>
            <div class="invalid-feedback">Это поле необходимо заполнить</div>
        </div>
        <div class="col-sm-4">
            <label class="form-label">Отчество (если есть)
                <input type="text" class="form-control">
            </label>
        </div>
        <div class="col-12">
            <label class="form-label">Организация (краткое название университета/школы)
                <input type="text" class="form-control" placeholder value required>
            </label>
            <div class="invalid-feedback">Это поле необходимо заполнить</div>
        </div>
        <div class="col-12">
            <label class="form-label">Курс (класс)
                <input type="text" class="form-control" placeholder value required>
            </label>
            <div class="invalid-feedback">Это поле необходимо заполнить</div>
        </div>
        <div class="col-12">
            <label class="form-label">Telegram-id (предпочтительно) или адрес электронной почты
                <input type="text" class="form-control" placeholder value required>
            </label>
            <div class="invalid-feedback">Это поле необходимо заполнить</div>
        </div>
    </div>
    <hr class="my-5">
    <select class="form-select mb-5">
        <option selected disabled>Формат участия</option>
        <option value="1">Докладчик</option>
        <option value="2">Гость</option>
    </select>
    <p class="fs-6 my-3"><strong>
        Область научного знания, к которой относится тема Вашего доклада:
    </strong></p>
    <div class="custom-form-check border border-2 border-rounded">
        <label class="custom-control-label p-2">
            <input class="custom-control-input" type="checkbox" value="">
            Биофизика/биотехнологии
        </label>
    </div>
    <hr class="my-5">
    <button class="btn btn-primary btn-lg" type="submit">Сохранить</button>
</form>
</div>
</div>
{% endblock %}
