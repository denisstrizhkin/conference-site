{% extends "schedule_base.jinja" %}

{% block title %}{{title}}{% endblock %}

{% block fill %}
<div class="card text-center" style="width: 40rem;">
    <h4 class="card-header">Вход</h4>
    <div class="card-body">
        <form>
            <div class="mb-3">
                <label for="exampleInputLogin" class="form-label">Login</label>
                <input type="login" class="form-control" id="InputLogin" aria-describedby="loginHelp">
                <div id="loginHelp" class="form-text">Выбирайте логин так, чтобы его не забыть к следующему разу!</div>
            </div>
            <div class="mb-3">
                <label for="InputPassword1" class="form-label">Password</label>
                <input type="password" class="form-control" id="exampleInputPassword1">
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div> 
</div>
{% endblock %}
